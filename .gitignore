# Dependencies
node_modules/

# Environment
.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# SQLite database files (main file and WAL/SHM sidecars)
data/data/*.sqlite*

# Uploaded assets
public/uploads/*
!public/uploads/.gitkeep
.vscode/settings.json
esp32_door_lock/config.h
data/*
.DS_Store
data/data/gmgmt.sqlite
esp32_door_lock/build/**

# Face model deployment artifacts (P1 outputs, ~40MB of binaries + deployment-
# specific manifest) — deployed into public/models/, never committed.
public/models/

# Local dev-server launch config — holds machine-specific runtimeExecutable
# paths (e.g. an absolute nvm node binary), so it must not be committed.
.claude/launch.json
